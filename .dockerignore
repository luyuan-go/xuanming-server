# Pandora go 服务镜像构建上下文裁剪(构建上下文 = 仓库根)。
# 保留 go.work 引用到的 Go module 源码(pkg / proto / services / robot/stress),
# 排除产物、缓存、IDE、文档和压测日志。

.git/
.github/
.vscode/
.idea/

# 构建产物 / 运行期目录
run/
**/bin/
**/*.exe
**/*.test
**/*.out

# 迁移凭据 / 真实目标 inventory 永不进入 BuildKit context 或中间层。
**/*.dsn
tools/migrate/*.json

# 部署产物(镜像不需要把部署目录再拷回去)
deploy/ds/stage/
deploy/dev-ca/
deploy/envoy/cert.pem
deploy/envoy/key.pem

# Python 侧实现(strangler 迁移,与 Go 版并存)。
# Go 服务镜像里一个字节都用不到它,但它进构建上下文的代价不是"多拷点文件" ——
# 改任何一个 .py 都会让 BuildKit 的上下文哈希变化,击穿 go mod download 那层缓存,
# 于是每次动 Python 代码,22 个 Go 服务镜像都要重下一遍依赖。
# .venv 有近两千个文件,更是绝不能进上下文。
python/

# 文档 / 杂项
docs/
robot/logs/
*.md
LICENSE

# OS 杂物
**/.DS_Store
Thumbs.db

# 交接:Python 栈跑通「登录 → 大厅 → 匹配 → 战斗 → 回大厅」

> **任务型文档,做完即删。** 长期规范进 `docs/design/python-migration.md`,别写这里。
> 接手前按 `AGENTS.md §1` 读 `PROGRESS.md` / `CLAUDE.md`。本文只覆盖这一件没做完的事。
>
> 最后更新:2026-08-24

---

## 0. 三十秒版本

目标:**Python 实现的 22 个服务撑起真实客户端玩一局**(不是单测、不是对拍,是人真的进去)。

现在到哪了:

| 环节 | 状态 |
|---|---|
| 22 个 Python 服务起来 | ✅ 一键脚本,`READY 22/22` |
| 客户端登录 | ✅ |
| 选角 → 拿大厅票 → ClientTravel | ✅ |
| **真进大厅 MainCity**(真 DS、真握手、真准入 ACK) | ✅ 已实测 |
| 组队 / SetReady / StartMatch | ✅ 全 200 |
| **进战斗** | 🟡 **阻塞已落码修复,等真机复测 —— 见 §2** |
| 战斗结算 / 退回大厅 | ⚠️ 后端链路已验(`tools/e2e_roundtrip.py` 全绿),真客户端还没走到 |

**唯一挡路的两个洞都已补上并有测试(14 条新用例)。剩下的只有一件事:
让客户端再走一次「登录 → 组队 → StartMatch」,看能不能真的进副本。**

---

## 1. 怎么跑

```bash
# 起(双击也行):基础设施 → 库结构升级 → 22 个 Python 服务
Python一键启动.cmd

# 停:服务 → 本机 DS → 大厅分片镜像 →(可选)容器
Python一键停止.cmd
```

常用参数:`-SkipInfra` 只重启业务服务、`-Exclude inventory` 留一个给断点调试、
`-LocalOnly` 把客户端面收回本机、`-KeepDs` 保留 DS 不杀。

全链验收(不用客户端):

```bash
cd python && $env:PANDORA_E2E_NO_FAKE_DS=1; .venv\Scripts\python.exe tools\e2e_roundtrip.py
```

- 大厅 DS 是**懒启动**的:双击启动完 DS 进程本来就是 0,要等第一个客户端登录触发
  `AssignHub` 才拉起。Go 栈同样如此,不是坏了。
- DS **没有控制台窗口**,而且**不许给它开**(见 §4 第 1 条:`-log` 会因 Windows
  快速编辑模式一点就冻死整个游戏线程)。日志落在**两个**目录(allocator 的
  `local.log_dir` 是相对路径,相对的是各自服务的工作目录):
  `services/battle/hub_allocator/run/dev/logs/ds/`(大厅)与
  `services/battle/ds_allocator/run/dev/logs/ds/`(战斗)。
- 但「大厅服起没起来 / 匹配之后战斗服有没有被拉起来」光看就绪表是看不出来的,所以
  一键启动现在会**另弹一个「DS 日志窗口」**:它是 `python/tools/tail_ds_logs.py`,
  **另一个进程只读跟随日志文件**,在里面点选/关掉都碰不到 DS。
  不想要就加 `-NoDsWindow`;想单独开:

  ```bash
  cd python && .venv\Scripts\python.exe tools\tail_ds_logs.py             # 只跟这一轮新起的 DS
  cd python && .venv\Scripts\python.exe tools\tail_ds_logs.py --recent 2  # 再带上最近 2 份历史
  ```

  窗口里 `>>> DS 上线:pandora-hub-local-*` 出现 = 大厅服起来了;匹配成功后会再冒出
  一个战斗 DS 的 pod,那就是进战斗那一步真的被拉起来了。
  两个细节是踩出来的,别改:①**要同时盯那两个目录**,只盯一个的表现是窗口永远
  空着,而人会把它读成"大厅服没起来";②**默认整批跳过已存在的历史日志**
  (实测那两个目录里躺着 1178 + 78 份),否则开窗就刷上千条横幅。

---

## 2. ★ 原阻塞:battle resume 少了 match_id(**已落码,待真机复测**)

### 现象(2026-08-23 实测)

客户端匹配成功后进不去,日志连刷三次然后放弃回登录界面:

```
TARGET carried an incomplete owner identity; retrying fail-closed:
  route=2 owner_epoch=5 op_id=1 pod=1 uid=1 inst_epoch=1 track=1 placement=1 hub_assign=0 alloc=1 match=0
TARGET stayed incomplete for 3 consecutive polls; giving up early
返回登录界面: reason=authority_entry_terminal
```

⚠️ **`hub_assign=0` 不是缺陷,别去补它。** 客户端 `HasCompleteOwnerIdentity`
(`MyDsRecoveryCoordinator.cpp:1968`)要求 `hub_assignment_id` 与 `allocation_id`
**按 route 严格二选一**,两者同时有值反而算「响应自相矛盾」:

```cpp
if (Route == Hub)    return !HubAssignmentId.IsEmpty() && AllocationId.IsEmpty();
if (Route == Battle) return  HubAssignmentId.IsEmpty() && !AllocationId.IsEmpty();
```

route=2(BATTLE)下 `hub_assign=0 alloc=1` 正是**合法**形态。真正缺的**只有 `match=0`**
一项 —— `HasCompleteOwnerTarget` 对 BATTLE 额外硬要求 `MatchId > 0`。

### 根因(两个洞,都是「Go 有、Python 整段没移植」)

**洞一:撮合富化缺失 → 进不去战斗。**
owner 只回答「这个玩家归谁管」,**不回答 match_id**;两侧 `applyOwnerPlacement` /
`apply_owner_placement` 都不设 `MatchID`,这是**两栈共同设计**,别去改 owner。
Go 在 owner 定完路由后有 `enrichResumeFromMatchAuthority` 从撮合权威补
`MatchID / MatchStage / GameMode / MapID`(只在字段为空时填),Python 侧 `grep enrich_resume`
零命中 —— 函数连同它的 3 个调用点整体没移植。

**洞二:首次进场链缺失 → 被踢回登录后就再也进不来。**
被踢回登录后重新登录,日志变成另一条死循环:

```
Login succeeded: hub_endpoint=missing
incomplete authoritative entry contract; retrying fail-closed   ← 无限重复
```

owner 说「无归属」时 Python 的 `get_resume_context` 回一个裸 `ResumeContextResult()`
(entry_state=UNSPECIFIED),而客户端 `PrepareAuthoritativeEntry` 的 `default` 分支
**明文规定服务端所有路径都必须给出五态之一**,拿到 UNSPECIFIED 就判契约被破坏、
fail-closed 无限重查。Go 那里是 `resolveFirstEntry`:角色门 → 分配首个 Hub → 回查 owner
→ 富化 → 给 exact TARGET。

### 佐证:调用点数量对不上(修复前)

| Go (`internal/biz/login.go`) | Python (`services/login/biz.py`) |
|---|---|
| 847 `enterRoleSession` | 1296 `_enter_role_session` ✅ |
| 1029 `buildBattleResume` | 1536 `_build_battle_resume` ✅ |
| 1412 `GetResumeContext` | 2512 `get_resume_context` ✅ |
| **1482 `resolveFirstEntry`** | **无** ❌ |

### 已经怎么修的

全部在 `python/pandorapy/services/login/biz.py`:

1. 新增 `_enrich_resume_from_match_authority(player_id, out)`,逐条对齐 Go:
   **只在字段为空时填**,**绝不改** route / target / owner_epoch / entry_state;
   非 ACTIVE claim 不富化;resolver 为 None 或调用失败 → 原样返回(降级成字段缺失)。
2. 新增 `_resolve_first_entry(player_id, sess_jti)`,对齐 Go `resolveFirstEntry`:
   角色不可判定 → WAIT/ROLE_UNKNOWN;role=0 → ROLE_REQUIRED(**不分配 Hub**);
   role>0 → `resolve_hub_endpoint` → 回查 owner → 富化;分配完仍查不到归属 → WAIT/OWNER_UNKNOWN。
3. 接上 3 个调用点(与 Go 一一对应):`get_resume_context` 的 TARGET 分支、
   `get_resume_context` 的「无归属」分支(改为走首次进场链)、登录路径 `out.resume = owned`
   那一处(对齐 Go `login.go:878`)。

修完调用点数已与 Go 对齐:`_resolve_resume_from_owner` **4 处**、富化 **3 处**。

### 已验证 / 未验证(别混为一谈)

**已验证**:

- 新增 `python/tests/test_login_resume_enrich.py` **14 条全绿**,含最小复现
  `test_battle_target_gets_match_id_filled`(owner 给 BATTLE TARGET 且 match_id=0
  → 富化后 match_id 必须非 0)。
- login/resume/owner 相关既有用例 **476 passed**,零回归。
- Python 全量 **5058 passed / 2 failed**;这 2 条与本改动无关且已逐条查实:
  `test_inventory_shop::test_real_dist_duplicate_entry_is_fail_closed` 是它**自己的前提断言**
  (「真实表里存在重复档」不再成立)、属于另一批 shop/currency 改动;
  `test_guild_service::test_dev_yaml_loads_and_session_gate_is_modeled` **单跑是绿的**,
  只在全量里红 = 用例间顺序污染。
- 一个曾经会让富化白做的疑点已查实**不成立**:login 只连 5v5 那台 matchmaker(20011),
  而这局是 `pve_coop`。但 `ResolvePlayerMatchContext` 的实现明写着「queue 与 active ZSET
  **刻意排除**……让 PVP 实例也能解析出一场 PVE match(两个模式共享 canonical 记录)」,
  所以 20011 那台对 PVE claim 同样能解析。

**未验证**:**真客户端还没再走一次匹配**。这是下一步唯一要做的事 ——
起栈 → 客户端登录 → 组队 → StartMatch,看 DS 日志窗口里是否冒出战斗 DS 的 pod、
客户端是否真的进了副本。不用重启客户端,后端改完重起 login 即可。

---

## 3. 这一轮已经修掉的(别再查这些)

| # | 问题 | 修法 | 影响面 |
|---|---|---|---|
| 1 | **DS 卡死在往控制台写日志** | `-log` → `-stdout -FullStdOutLogOutput` | Go/Python × hub/battle **四处**都改了 |
| 2 | kafka `socket_timeout_ms` 不是合法参数 | `kafkax.py` 改 `request_timeout_ms`;`dial_timeout` 改 `bootstrap_timeout_ms` | 5 个服务原本启动即退 |
| 3 | Envoy 客户端面只绑 127.0.0.1 | 一键脚本导出 `PANDORA_EDGE_BIND_HOST=0.0.0.0` | 客户端连内网 IP 被 Connection refused |
| 4 | `player_wallet` 表名(Go 侧多币种批次) | `currency.go` 六处 + budgets + dbcheck + infra.md | 真库上直接 1054 |
| 5 | 迁移器陈旧导致库半迁移 | 重建 `pandora-migrate.exe` | 它会自洽地报「已是最新」 |
| 6 | ds_allocator 取消时泄漏 warming pod | `safego.detach` + 三处补偿 | Go 会回收,Python 不会 |
| 7 | 一键脚本三个 bug | 见 §5 | |

**#1 的证据**(cdb 非侵入抓栈,游戏线程):

```
FWindowsConsoleOutputDevice::Serialize  ← 阻塞
  ← UIpNetDriver::TrackAndLogNewIP      ← 第一个客户端连入时打的那行
  ← TickDispatch ← UWorld::Tick ← FEngineLoop::Tick
```

Windows 控制台快速编辑模式下,**在黑窗口里点一下就能冻死整个 DS**。

---

## 4. ★ 我判错的地方(别沿用我的结论)

写完本文后发现**另一个会话(originSessionId `8608f78d`)已经把其中两条查得更深并定谳了**,
证据在 Prometheus,以对方为准。记忆里可查
`hub-heartbeat-slow-owner-lease-20260824` / `local-hub-credential-ack-after-backend-restart-20260824`。

1. **`credential ACK missing`** —— 我判成「几小时前旧 DS 的残留,不是病因」,方向对但太浅。
   真根因:`mode=local` 下 DS 身份只活在 hub_allocator **进程内存**里(`local_fleet.py:183` = Go
   `local_fleet.go:140`,都是 `uuid[:8]`),**后端一重启就换新 pod 名**,存活的旧 DS 永久失配,
   20s 后自我 fencing。而且**不自愈**:旧 DS 心跳仍然成功 → 分片一直 ready → 粘性归属命中它 →
   AssignHub 走复用分支 → 永不触发拉起带正确凭据的新 DS。**最小恢复 = 重启那台 DS,改代码没用**
   (Go 版逐字段同构)。我说的「ACK 五元组是满的」只在**当前后端亲手拉起的** DS 上成立;
   失配态下五元组停在 proto3 零值,而 envoy 那边照样是 200 —— **「后端 200」不等于「ACK 正常」**。
   → 一键停止脚本里「全杀 DS」正是这条的预防措施,别为了省事把它删掉。
2. **DS 心跳偏慢 1.5~4s** —— 我归因成「我并行跑 12 路 `cl.exe` 出包造成 CPU 饥饿」,**不成立**。
   真根因:hub 心跳**同步 await** `owner.RenewInstanceLease`(2s 超时),owner 每次一笔 MySQL 事务,
   而 MySQL 所在 Docker VM 周期性秒级 I/O 停顿。Go 版同构,不是 Python 引入的。
   附带一条通用教训:**envoy 的 `upstream_service_time` 在本栈结构性无效** —— TraceInterceptor 挂最外层、
   在 handler 之前就 `send_initial_metadata`,那一列恒 ~1ms。要看后端真耗时用
   `pandora_rpc_duration_seconds_{sum,count}` 按桶算 Δsum/Δcount。
3. **`LocalHubCredentialSource` 少了 `@runtime_checkable`** —— 这条确实是我猜错,可放心跳过:
   实测 `_is_runtime_protocol=True`,`isinstance` 正常,`LocalHubFleetProvider` 也有那个方法。

另外这两条**不是缺陷,是 mock 模式的固有限制**,别当 bug 修:

- mock 模式下 `AllocateBattle` 恒 `owner_not_exact` / 15005 —— 产不出 exact 实例身份,
  Go 完全一样。要验 happy path 得把 `owner_addr` 置空,或用 local 模式。
- `probe_ds.py` 场景 06/07/08(非法 map_id / game_mode)在 mock 下恒回 OK ——
  关卡表只在 local/agones 起真 DS 拼地图命令行时才被查。已写进探针注释。

---

## 5. 环境坑(踩过,写下来省时间)

- **大厅分片镜像会卡住懒启动**:`ensure_shards` 判「该 region 有任意分片记录就提前返回」,
  **不看是不是 draining**,于是走不到 `fleet.list_shards()`(懒拉起 DS 的地方)。
  旧 DS 留下的镜像带 30 分钟 TTL → `AssignHub` 恒 `ERR_HUB_NO_AVAILABLE` 且**日志零输出**。
  一键脚本现在停止和启动前都清 `pandora:hub:shard:{pandora-hub-local-*}`。**Go 栈同样有这个坑,还没补。**
- **启停顺序不能反**:必须「先停服务 → 再全杀 DS → 再清镜像 → 最后起」。
  先杀 DS 的话它父进程还活着、不算孤儿、逃过一劫,等 allocator 被停掉才变孤儿,
  继续占着 7777 用**旧 pod 身份** → 客户端拿新票撞旧 DS → `admission is no longer current`。
- **PowerShell 函数会吞输出**:`$code = Invoke-RunStack ...` 把就绪表一起吸进 `$code`,
  表看不见、`$code` 变数组让 `-ne 0` 恒真 → 22/22 全绿却报失败。必须 `Out-Host`。
- **UE 引擎有两个根**:工程 `EngineAssociation` 是 GUID,注册表指向
  `F:\UnrealEngine-5.8.0-release`(源码引擎)。用 `E:\Program Files\UE_5.8` 会报
  「Incompatible or missing module」58 个 —— 那**不是**没编译,是指错引擎。
- **全量出包会失败在测试模块**:`UnrealEditor-PandoraTests.dll` 有两个未解析符号
  (`FCfgTableRow::ToString`、`UMyEntityAttrSet::ResolveHpAfterMaxHpChange`)。
  DS 二进制本身链接成功,所以走 `Package_Server_Win64_Dev_AssetOnly.bat` 可以出包。**这两个符号还得有人补。**

---

## 6. 本轮改动的文件

**Python(本次阻塞修复)**:`services/login/biz.py`(+`_enrich_resume_from_match_authority`
/ `_resolve_first_entry` / 3 处调用点)、**新增** `tests/test_login_resume_enrich.py`、
**新增** `tools/tail_ds_logs.py`、`tools/run_stack.py`(+`--ds-window`)、
`tools/scripts/dev_all_python.ps1`(+`-NoDsWindow`)

**Python(前几轮)**:`kafkax.py`、`safego.py`、`errcode.py`(重生)、
`services/ds_allocator/{biz_allocate,service,local_allocator}.py`、
`services/hub_allocator/local_fleet.py`、`tools/parity/probe_{hub,ds}.py`、
**新增** `tools/run_stack.py`、**新增** `tools/e2e_roundtrip.py`

**Go / 基础设施**:`hub_allocator/internal/biz/local_fleet.go`、
`ds_allocator/internal/data/local_allocator.go`、
`inventory/internal/data/{currency,budgets}.go`、`inventory_retention_mysql_test.go`、
`tools/migrate/cmd/dbcheck/main.go`、`docs/design/infra.md`、`deploy/docker-compose.dev.yml`、
`deploy/env/dev.env`(gitignore)

**新增双击入口**:`Python一键启动.cmd`、`Python一键停止.cmd`、
`tools/scripts/dev_all_python.ps1`

**测试**:Python 全量最近一次干净跑 **5059 passed / 0 failed / 0 skipped**;
Go hub/ds allocator 与 inventory 各包全绿(含真 MySQL)。
改 `-log→-stdout` 后四处参数顺序断言已同步更新,并在测试里写明**不能改回 `-log`** 及理由。

---

## 7. 协作注意

- 客户端仓库(`F:\work\Pandora-Client-SVN`)这段时间**有另一个会话在实时改**
  (多币种 proto 跟进、`BindKey` 修复等)。动客户端代码前先看 `svn status` 和文件 mtime,
  别撞车。本轮我在客户端只做过一次改动,已 `svn revert`,**目前客户端工作副本无我的改动**。
- 改客户端代码前必须先看 `Pandora-Client-SVN/Doc/客户端/规范`(那边 CLAUDE.md 的硬要求)。
- ❌ 不许 `git push` / `git tag`;`git commit` 需用户明说。
- ❌ 不许把「静态看过了」写成「验证过了」。
